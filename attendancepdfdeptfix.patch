commit ee7586ab59792459ea440d91f4a90a06ee16a4bb
Author: Claude <noreply@anthropic.com>
Date:   Thu Jul 23 17:09:23 2026 +0000

    考勤分析PDF导出改为按部门/科室层级展示
    
    与离职率分析报表保持一致：每个部门下嵌套显示各科室的出勤数据，替代原先按 anaGrp 单一维度平铺的表格。

diff --git a/index.html b/index.html
index ed11729..493b3d9 100644
--- a/index.html
+++ b/index.html
@@ -4964,29 +4964,51 @@ window.chartToImg=function(chart,caption){if(!chart)return null;try{return{src:c
 function exportAttAnalysisPDF(){
   const E=s=>String(s==null?'':s).replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));
   const secF=$('ana-dept-f').value;let emps=mxEmps().filter(isSkillPos);if(secF)emps=emps.filter(e=>e.section===secF);
-  const groups={};
-  if(anaGrp==='dept'){emps.forEach(e=>{const k=e.dept||'未分配';(groups[k]=groups[k]||[]).push(e);});}
-  else{emps.forEach(e=>{const k=e.section||'未分科室';(groups[k]=groups[k]||[]).push(e);});}
-  let rows=Object.keys(groups).map(name=>({name,...computeWeekStats(groups[name],anaWeekOff)})).sort((a,b)=>b.attRate-a.attRate);
-  if(anaExclBlank)rows=rows.filter(r=>r.emp>0);
-  const grpLbl=anaGrp==='dept'?'部门':'科室';
-  const tot=rows.reduce((a,r)=>({emp:a.emp+r.emp,aDays:a.aDays+r.aDays,expTotal:a.expTotal+r.expTotal,absDays:a.absDays+r.absDays,fiDays:a.fiDays+r.fiDays}),{emp:0,aDays:0,expTotal:0,absDays:0,fiDays:0});
-  const attRate=tot.expTotal?(tot.aDays/tot.expTotal*100):0,fiRate=tot.expTotal?(tot.fiDays/tot.expTotal*100):0;
-  let html=`<table><thead><tr><th>${grpLbl}</th><th>人数</th><th>出勤天数(A)</th><th>应出勤天数</th><th>出勤率</th><th>缺勤天数</th><th>旷工天数(FI)</th><th>旷工占比</th></tr></thead><tbody>`;
-  html+=rows.map(r=>`<tr><td>${E(r.name)}</td><td>${r.emp}</td><td>${r.aDays}</td><td>${r.expTotal}</td><td>${r.attRate.toFixed(1)}%</td><td>${r.absDays}</td><td>${r.fiDays}</td><td>${r.fiRate.toFixed(2)}%</td></tr>`).join('');
-  html+=`<tr style="font-weight:700;background:#f8fafc"><td>合计</td><td>${tot.emp}</td><td>${tot.aDays}</td><td>${tot.expTotal}</td><td>${attRate.toFixed(1)}%</td><td>${tot.absDays}</td><td>${tot.fiDays}</td><td>${fiRate.toFixed(2)}%</td></tr></tbody></table>`;
+  // 按部门→科室分组（与页面表格一致，参照离职率分析的报表结构）
+  const byDept={};
+  emps.forEach(e=>{
+    const d=e.dept||'未分配';
+    const s=(e.section&&e.section!==e.dept)?e.section:'__none__';
+    if(!byDept[d])byDept[d]={dept:d,emps:[],secs:{}};
+    byDept[d].emps.push(e);
+    if(!byDept[d].secs[s])byDept[d].secs[s]=[];
+    byDept[d].secs[s].push(e);
+  });
+  let deptList=Object.keys(byDept).map(d=>({...byDept[d],st:computeWeekStats(byDept[d].emps,anaWeekOff)}));
+  if(anaExclBlank)deptList=deptList.filter(d=>d.st.emp>0);
+  deptList=deptList.sort((a,b)=>b.st.attRate-a.st.attRate);
+  const tot=computeWeekStats(emps,anaWeekOff);
+  let html=`<table><thead><tr><th>部门</th><th>科室</th><th>人数</th><th>出勤天数(A)</th><th>应出勤天数</th><th>出勤率</th><th>缺勤天数</th><th>旷工天数(FI)</th><th>旷工占比</th></tr></thead><tbody>`;
+  deptList.forEach(d=>{
+    const secs=d.secs;const realSecs=Object.keys(secs).filter(k=>k!=='__none__');const hasSecs=realSecs.length>0;
+    const r=d.st;
+    html+=`<tr style="font-weight:700;background:#eef2ff"><td>${E(d.dept)}</td><td style="text-align:left">${hasSecs?'部门汇总':''}</td><td>${r.emp}</td><td>${r.aDays}</td><td>${r.expTotal}</td><td>${r.attRate.toFixed(1)}%</td><td>${r.absDays}</td><td>${r.fiDays}</td><td>${r.fiRate.toFixed(2)}%</td></tr>`;
+    if(hasSecs){
+      const secRows=realSecs.map(sec=>({sec,st:computeWeekStats(secs[sec],anaWeekOff)})).sort((a,b)=>b.st.attRate-a.st.attRate);
+      secRows.forEach(s=>{
+        if(anaExclBlank&&s.st.emp===0)return;
+        const st=s.st;
+        html+=`<tr><td></td><td style="text-align:left">└ ${E(s.sec)}</td><td>${st.emp}</td><td>${st.aDays}</td><td>${st.expTotal}</td><td>${st.attRate.toFixed(1)}%</td><td>${st.absDays}</td><td>${st.fiDays}</td><td>${st.fiRate.toFixed(2)}%</td></tr>`;
+      });
+      if(secs['__none__']){
+        const st=computeWeekStats(secs['__none__'],anaWeekOff);
+        if(!anaExclBlank||st.emp>0)html+=`<tr><td></td><td style="text-align:left">└ （部门直属）</td><td>${st.emp}</td><td>${st.aDays}</td><td>${st.expTotal}</td><td>${st.attRate.toFixed(1)}%</td><td>${st.absDays}</td><td>${st.fiDays}</td><td>${st.fiRate.toFixed(2)}%</td></tr>`;
+      }
+    }
+  });
+  html+=`<tr style="font-weight:700;background:#f8fafc"><td>合计</td><td style="text-align:left"></td><td>${tot.emp}</td><td>${tot.aDays}</td><td>${tot.expTotal}</td><td>${tot.attRate.toFixed(1)}%</td><td>${tot.absDays}</td><td>${tot.fiDays}</td><td>${tot.fiRate.toFixed(2)}%</td></tr></tbody></table>`;
   buildPDFReport({
     title:'考勤分析报表',
-    meta:`统计周期：${wLabel(anaWeekOff)}　·　分组：按${grpLbl}${secF?'　·　科室：'+secF:''}　·　仅技能职员工　·　生成时间：${new Date().toLocaleString('zh-CN')}`,
+    meta:`统计周期：${wLabel(anaWeekOff)}　·　以科室为单位（无科室按部门）${secF?'　·　科室：'+secF:''}　·　仅技能职员工　·　生成时间：${new Date().toLocaleString('zh-CN')}`,
     sections:[
       {kind:'kpi',cards:[
         {label:'统计人数',value:tot.emp+' 人'},
-        {label:'平均出勤率',value:attRate.toFixed(1)+'%',color:'#1f8a5b'},
-        {label:'平均旷工占比',value:fiRate.toFixed(2)+'%',color:'#c0392b'},
+        {label:'平均出勤率',value:tot.attRate.toFixed(1)+'%',color:'#1f8a5b'},
+        {label:'平均旷工占比',value:tot.fiRate.toFixed(2)+'%',color:'#c0392b'},
         {label:'旷工总天数(FI)',value:tot.fiDays,color:'#c0392b'},
       ]},
-      {kind:'table',title:grpLbl+'考勤分析',html:html},
-      {kind:'charts',title:'图表',cols:2,images:[chartToImg(charts.ana1,'各'+grpLbl+'出勤率对比'),chartToImg(charts.ana2,'各'+grpLbl+'旷工占比对比')]},
+      {kind:'table',title:'各部门/科室考勤分析',html:html},
+      {kind:'charts',title:'图表',cols:2,images:[chartToImg(charts.ana1,'各部门/科室出勤率对比'),chartToImg(charts.ana2,'各部门/科室旷工占比对比')]},
       {kind:'charts',cols:1,images:[chartToImg(charts.ana3,'工厂近 6 周 — 出勤率 vs 旷工占比')]},
     ]
   });
